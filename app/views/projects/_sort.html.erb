    <li class="nav-item ms-auto">
      <div class="dropdown">
        <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          Sort
        </button>
        <ul class="dropdown-menu">
          <li><a class="dropdown-item <%= 'active' if params[:sort] == 'ecosystem' %>" href="<%= url_for(sort: 'ecosystem', order: 'asc') %>">Ecosystem</a></li>
          <li><a class="dropdown-item <%= 'active' if params[:sort] == 'name' %>" href="<%= url_for(sort: 'name', order: 'asc') %>">Name</a></li>
          <li><a class="dropdown-item <%= 'active' if params[:sort] == 'mentions_count' %>" href="<%= url_for(sort: 'mentions_count', order: 'desc') %>">Mentions</a></li>
        </ul>
      </div>
    </li>